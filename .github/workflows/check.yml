# master に入る前に見る。壊れたものが公開されてから気づくのでは遅い。
# 出す係は deploy.yml。
name: check

on:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          # 既定では GITHUB_TOKEN が .git/config に残り、依存パッケージの
          # postinstall から読める。ここでは push しないので持たせない
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run lint
      - run: npm run build
